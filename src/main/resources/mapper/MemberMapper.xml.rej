diff a/src/main/resources/mapper/MemberMapper.xml b/src/main/resources/mapper/MemberMapper.xml	(rejected hunks)
@@ -1,5 +1,21 @@
 <?xml version="1.0" encoding="UTF-8"?>
 <!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
 <mapper namespace="com.app.osca.mapper.MemberMapper">
+    <!-- 1. 회원의 이름, 이메일, 핸드폰 번호 가져오기 -->
+    <select id="select" resultType="memberVO">
+        SELECT ID, MEMBER_NAME, MEMBER_EMAIL FROM TBL_MEMBER
+        WHERE ID = #{id}
+    </select>
 
+    <!-- 2. 회원의 이름, 핸드폰 번호 수정하기 -->
+    <update id="update">
+        UPDATE TBL_MEMBER
+        SET MEMBER_NAME=#{memberName}
+        WHERE ID = #{id}
+    </update>
+
+    <!-- 3. 회원 탈퇴하기 -->
+    <delete id="delete">
+        DELETE FROM TBL_MEMBER WHERE ID = #{id}
+    </delete>
 </mapper>
\ No newline at end of file
